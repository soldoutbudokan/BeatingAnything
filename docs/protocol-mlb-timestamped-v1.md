# F1: Frozen MLB model on newly located timestamped FanDuel observations

Recorded 2026-09-08 before retrieving 2026 sport outcomes or evaluating model returns on this source. The source audit has inspected only odds schemas, timestamps and coverage. This is an additional retrospective test, not a prospective registration. The previously inspected 2024/25 outcomes remain development knowledge and cannot become untouched evidence again.

## Fixed scope and forecasts

Use the first downloaded, hashed public MLB moneyline CSV from The Odds Gap's seven-day research export, covering September 1–8, 2026. Do not extend or replace this snapshot based on results. Preserve the file locally with its SHA-256; publish derived metrics and attribution rather than redistributing the raw feed. The publisher attributes sportsbook odds to The Odds API. Capture timestamps are publisher observations, not bookmaker freshness or proof of execution.

The research model is the unchanged MLB physical artifact `a7ceefe2db3c13ea`, trained through 2025-08-16, already present before locating this source. Its coefficients and 21 feature definitions remain unchanged. Controls are FanDuel proportional no-vig probabilities and, where available in the exact same publisher snapshot, Pinnacle proportional no-vig probabilities. Pinnacle is an untrained pricing control, not a sport model or a replacement FanDuel entry. Report each and never choose a model from these results.

Join exact home/away identities and scheduled starts against official MLB schedules. Permit only an explicitly documented Athletics name alias. Require a unique official game within 15 minutes of the source scheduled start; reject ambiguous team/start identities. Exclude doubleheaders and non-nine-inning/nonregular fixtures using schedule metadata. Preserve missing outcomes and cancellations in the accepted forecast population, and record any unresolved grading. Do not use results or closing availability to choose entries.

For each event select the earliest valid paired FanDuel snapshot between six hours and 30 minutes before its source scheduled start, on the same America/New_York calendar date as the official game date. Exact timestamp, book, event, market and both sides must agree; conflicting duplicate side rows invalidate the pair. Require finite decimal prices >1 and paired overround [0, 0.08]. Choose this snapshot before looking at model EV; later more favorable snapshots cannot replace it. Preserve audit counts for every rejection. No entry is added or removed because its close is missing.

Reconstruct official 2026 MLB features using the existing `mlb_lagged_workload_v1` formulas, withholding same-day results. All source retrievals and hashes are recorded. Historical official endpoints contain revisions and cannot certify publication vintage. Missing travel/bullpen inputs block the physical forecast and remain visible. Market controls are reported both on their own available population and the exact population shared with the physical model; differences on different populations must not be treated as paired improvement.

## Fixed policy and closing reference

One unit maximum per event. Choose the larger-EV side, home on ties, at model EV >=3%, chosen decimal odds [1.20, 6], entry overround [0,0.08]. No threshold fitting, stake optimization, best-book substitution or subgroup selection. Reduce net winnings 2% for execution sensitivity. Completed ordinary games are hypothetical full-game settlements; postponed, suspended, cancelled, tied, shortened or unclear games remain ungraded until the relevant book's grading rule is established.

Primary closing reference: last valid paired Pinnacle snapshot from 30 minutes to one minute before the earlier of official and source scheduled start. FanDuel same-window close is a robustness benchmark. Require pair overround [0,0.15]; pair selection never depends on the selected side or outcome. No after-start price or final archive field is allowed. A verified first-pitch timestamp, if available, can only tighten the prestart boundary, never extend it. These are near-start observed prices; do not label them exact closing ticks or verified executable quotes. Missing closes remain missing and block favorable evidence. Report proportional and power-method de-vig; neither is chosen by profitability.

Report accepted/missing/blocked counts, forecast and source hashes, all-event log loss/Brier, paired loss differences, selected bets and turnover, settlement coverage, full ROI or unresolved best/worst bounds, haircut ROI, daily drawdown, CLV coverage, selected mean no-vig closing EV, raw price ratios, and per-day descriptive results. Show the physical model's and Pinnacle control's results regardless of direction.

This seven-day sample cannot independently establish a betting edge. Do not produce a confidence interval from fewer than eight distinct calendar weeks; report the point estimate with an explicit insufficient-blocks status instead. If later separately frozen samples permit inference, use 10,000 weekly bootstrap samples, seed 1729 and a conservative thirteen-comparison allowance (eleven prior registered candidates plus these two new evaluations), confidence `1 - 0.05/13`. The existing prospective FanDuel qualification requirements remain unmet. No alert, wager, purchase or claim of victory follows from this pilot.
