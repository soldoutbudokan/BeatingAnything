# Card 39: fixed NBA FanDuel main-points consensus screen

Declared at 2026-09-13T18:30:17.404852+00:00, before computing any NBA price discrepancy or joining player scoring outcomes in this study. The earlier 20-file source audit inspected fields and clocks only. This file is immutable once hashed.

**Mechanism.** Book-specific prop shading or asynchronous repricing can leave FanDuel offering a higher return than contemporaneous paired prices from other books imply. Reference consensus is fallible; only settled actual returns can advance this hypothesis. This is a new NBA player-points question, distinct from the closed NFL main-line comparison.

**Fixed source and periods.** All `data/historical_points` JSONs in `devlincorrigan/nba-props-threshold-app@233dbbc86b9c6e13df04d4e8b063581b3aa15abb`, inventoried at 3,394 files, 397,273,601 bytes. Independently verify publisher event-to-game mappings with NBA Stats-derived team/date fixtures. Evaluate 2024–25 regular-season game IDs `00224*` first. Reserve represented 2025–26 regular-season game IDs `00225*` as a separate chronological check; freeze selections for both periods before evaluating any 2025–26 scores. The first period's outcomes were examined for unrelated prior research and are not an untouched holdout. An incomplete 2025–26 archive remains incomplete; do not add later snapshots or other years to reach a result.

**Quote eligibility.** Literal `player_points`, exact identical player/half-point line, exactly one Over and one Under. Decimal prices both within [1.20, 6.00]; pair overround in [0, .12]. Require unique FanDuel book plus at least three distinct other book keys satisfying the same pair and clock rules; include every eligible reference book. No future updates: BOTH book and market updates must be between zero and 300 seconds old relative to the original archive `timestamp`. Never shift that snapshot forward. Snapshot must precede both reported event start and independently verified fixture date/start. Reject ambiguous duplicates. Any schedule carrying only a game date permits the snapshot only when strictly before that NBA-local calendar game date, using America/New_York for the quote; record absence of independently recorded actual tip time. Matching published event home/away identities is mandatory. Conservative normalized full names remove Unicode accents, punctuation and whitespace only; do not drop suffixes, infer aliases or use later target/appearance outcomes for eligibility. A unique NBA person ID can be learned from identity-only columns across the independently sourced box archive, without requiring same-game appearance. Unknown or ambiguous identity is excluded before settlement.

**Fixed score and choice.** Proportional devig p_i=q_i/sum(q) and power devig p_i=q_i^k with sum(q_i^k)=1; calculate independently for each reference pair, then take the median side probability across all qualifying references for each method. A one-unit FanDuel win returns net .98*(decimal_price-1); a loss returns -1. Conservative expected return is the smaller of the two median-probability expected returns. Require >=.03. Select one side per NBA game: highest conservative score, then ascending stable person ID, then lexical side, then line. If multiple snapshots represent a game, preserve its earliest qualifying snapshot (stable event ID/path ties); never choose a later favorable price. Selections and all rejected/missing records are frozen and hashed before outcomes are joined. No fitted model, threshold adjustment, favored-book selection, subgroup rescue or post-result cohort expansion.

**Settlement and evidence gate.** Hypothetical standard full-game individual points including overtime. Half-point lines cannot push. DNP void only with affirmative, reliable box evidence and team box totals/minutes reconciliation; missing player row is unknown, not DNP. A named box row with blank/zero minutes must also have zero points and a DNP/inactive reason if available; distinguish rows with actual positive time. Incomplete/ambiguous games stay unsettled. This does not assert historical jurisdiction-specific terms, available fills, accepted stakes or wager placement. One unit per settled game; voids have zero stake for ROI denominator. Report wins/losses/voids/unsettled, unit profit, haircut ROI, raw ROI, maximum chronological drawdown and 95% percentile game bootstrap interval (10,000 replicates, seed 390013). A promising backtest requires >=100 settled independent games and haircut ROI >0 with bootstrap 95% lower bound >0; else failed/underpowered as appropriate. Evaluate first period and write its report before unlocking reserved outcomes. The same gate applies independently to the reserved period. Even passing both is research evidence requiring independent/forward prices and project validation, not a verified live edge. Single early snapshots lack same-line closes and suspension/fill evidence; record these gaps.
